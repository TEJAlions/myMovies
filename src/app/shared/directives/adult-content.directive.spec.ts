import { AdultContentDirective } from './adult-content.directive';

describe('AdultContentDirective', () => {
  it('should create an instance', () => {
    const directive = new AdultContentDirective();
    expect(directive).toBeTruthy();
  });
});
